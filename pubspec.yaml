name: framework
description: This is the package which contains all most fequently using stuffs
version: 0.0.1
homepage:
environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"
dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^1.0.0
  logger: ^1.1.0
  intl: ^0.17.0
  shared_preferences: ^2.0.13
  get_it: ^7.2.0
  mocktail: ^0.3.0
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
